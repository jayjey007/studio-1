
rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /chat_images/{allPaths=**} {
      allow write: if request.auth != null;
      allow read; // Allow public read access to images
    }
  }
}
