
rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // Allow reads and writes to the chat_images directory for any authenticated user.
    match /chat_images/{allPaths=**} {
      allow read, write: if request.auth != null;
    }
  }
}

    