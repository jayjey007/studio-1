rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // Allow reads and writes on files in the chat_images directory
    // only if the user is authenticated.
    match /chat_images/{allPaths=**} {
      allow read, write: if request.auth != null;
    }
  }
}
